node_modules/
.next/
out/
build/
dist/

.env
.env.local
.env*.local

*.log
.DS_Store
.vscode/
.idea/

next-env.d.ts
tsconfig.tsbuildinfo

supabase/.branches
supabase/.temp

# MCP config holds a Supabase PAT. Stays local; do not commit.
.mcp.json

# Subagent worktrees (created by Claude Code's isolation: "worktree" option).
# These are ephemeral local copies of the repo — never committed.
.claude/worktrees/
