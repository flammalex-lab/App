// No-op stub for the `server-only` Next.js runtime guard so Jest can
// resolve modules that import it. The real package throws if imported
// from a client bundle, which is irrelevant in tests.
export {};
